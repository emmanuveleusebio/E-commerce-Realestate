/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['e0.pxfuel.com', 'stackup-bucket.s3.ap-south-1.amazonaws.com', 'www.shutterstock.com'],
    },
  };
  
  export default nextConfig;
  